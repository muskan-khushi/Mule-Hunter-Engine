server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve JSON files correctly
  location ~* \.json$ {
    default_type application/json;
    try_files $uri =404;
  }

  # React SPA fallback
  location / {
    try_files $uri $uri/ /index.html;
  }
}
