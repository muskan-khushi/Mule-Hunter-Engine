version: "3.8"

services:
  # 1. AI INTELLIGENCE ENGINE
  ai-engine:
    container_name: mule-hunter-ai
    build:
      context: ./ai-engine
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    volumes:
      - ./shared-data:/shared-data

  # 2. VISUAL ANALYTICS (Graph Service)
  visual-analytics:
    container_name: visual-analytics
    build:
      context: ./visual-analytics
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - INTERNAL_API_KEY=${INTERNAL_API_KEY:-${VISUAL_INTERNAL_API_KEY}}
      - BACKEND_BASE_URL=http://13.60.91.248:8082
      - REQUEST_TIMEOUT=60
    volumes:
      - ./visual-analytics/visualization:/app/visualization
      - ./visual-analytics/scripts:/app/scripts

  # 3. CONTROL TOWER (Frontend)
  control-tower:
    container_name: control-tower-app
    image: control-tower:latest
    build:
      context: ./control-tower
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=http://13.60.91.248:8082
        - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
        - NEXTAUTH_URL=${NEXTAUTH_URL}
        - JWT_SECRET=${JWT_SECRET}
        - NEXT_PUBLIC_EMAILJS_SERVICE_ID=${NEXT_PUBLIC_EMAILJS_SERVICE_ID}
        - NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=${NEXT_PUBLIC_EMAILJS_TEMPLATE_ID}
        - NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=${NEXT_PUBLIC_EMAILJS_PUBLIC_KEY}
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - NEXTAUTH_URL=${NEXTAUTH_URL}
      - JWT_SECRET=${JWT_SECRET}
      - MONGODB_URI=${MONGODB_URI}
